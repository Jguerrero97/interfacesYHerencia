package com.example;

/**
 * Hello world!
 */
public class App {
    public static void main(String[] args) {
        /*
         * Suponer la existencia de una fabrica ficticia de bicicletas,
         * donde se ensamblan bicis de montaña, de carrera, electricas,
         * etc.
         */

        /*
         * ¿Que es una Interface? Es un conjunto de metodos relaciones,
         * que pueden ser en primer lugar, abstractos (que no tiene cuerpo),
         * static, default.
         * Y desde el punto de vista teorico/funcional una Interface es un CONTRATO
         * que se establece mediante el cual la clase que implemente dicha interface
         * se compromete a implementar todos los metodos de la interface aunque solamente
         * vaya a utilizar uno solo.
         * Cuando la clase implemente los metodos de la interface, no puede violar/modificar
         * la firma del metodo
         * EXAMEN: ¿Que es la firma del metodo?
         * Es el nombre del metodo, lo que recibe como parametro, sin incluir lo que devuelve como parametro,
         * sin incluir lo que devuelve.
         */
    }
}
